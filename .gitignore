node_modules/
target/
dist/
src-tauri/target/
src-tauri/gen/
.env
.env.*
!.env.example
*.log
coverage/

# Local agent / IDE workspace dirs that occasionally drop symlinks
# pointing into $HOME — they're machine-specific and never belong in
# the repo.
.antigravitycli/
.gemini/
.cursor/
